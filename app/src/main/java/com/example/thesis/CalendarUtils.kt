package com.example.thesis

import java.time.*


object CalendarUtils {
    lateinit var selectedDate: LocalDateTime
    var studyStart = LocalTime.of(10, 0, 0)
    var studyEnd = LocalTime.of(20, 0, 0)
}